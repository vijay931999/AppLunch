# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AppTouchTask' do
  pod 'ReachabilitySwift'
  use_frameworks!
  
  # Pods for AppTouchTask
  
  target 'AppTouchTaskTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'AppTouchTaskUITests' do
    # Pods for testing
  end
  
end
